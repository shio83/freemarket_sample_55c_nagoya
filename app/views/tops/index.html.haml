.body
  .body__next
    .body__next--top
      .body__next--top--head
        = render partial: 'tops/header/head'
      .body__next--top--heading
        = render partial: 'heading'
        .body__next--top--heading--ave
          = render partial: 'ave'
        .body__next--top--heading--category
          = render partial: 'tops/items/category'
        .body__next--top--heading--items
          = render partial: 'tops/items/items'
        .body__next--top--header--bottom
          = render partial: 'bottom'
